---
title: "Loro – Reimagine state management with CRDTs"
keywords:
  "loro, crdt, state management, local-first software, real-time collaboration,
  rich text editor"
description:
  "Loro - Reimagine state management with CRDTs | Built for local-first
  software."
---

import Support from "@components/landing/Support";
import Features from "@components/landing/Features";
import FollowOnGitHub from "@components/landing/FollowOnGitHub";
import Hero from "@components/landing/Hero";
import Background from "@components/landing/Background";
import Footer from "@components/landing/Footer";
import { Separator } from "@ariakit/react";
import Demo from "@components/richtextDemo";

<div className="relative landing-page-root dark bg-zinc-900">
  <div style={{ maxWidth: 1300, margin: "0 auto" }}>
    <Hero />
    <div className="text-center text-white/95 text-base font-medium leading-normal mt-10">
      ✨ Loro 1.0 is released! <br/>
      Read the <a href="/blog/v1.0"><u>announcement</u></a> to learn more
    </div>
    <Separator
      className="mt-4 md:mt-10 mx-5 md:mx-15 border-t border-separator"
      orientation="horizontal"
    />
    <Features />

    <section className="px-5 md:px-15 mt-4 z-10 relative">
      <h2 className="text-4xl text-center not-italic font-semibold leading-[normal] bg-clip-text mb-16 bg-blue-green text-fill-transparent">
        Who's Using Loro
      </h2>
      <div className="grid grid-cols-2 md:grid-cols-3 gap-8 items-center">
        <div className="flex items-center justify-center p-4 rounded-lg hover:bg-zinc-800 transition-all duration-300 text-zinc-200">
          <a 
            href="https://latch.bio" 
            target="_blank" 
            rel="noopener noreferrer"
          >
            <img src="/images/latchbio.svg" alt="Latch.bio" className="h-12 brightness-[0.9] contrast-[1.1]"  />
          </a>
        </div>
        <div className="flex items-center justify-center p-4 rounded-lg hover:bg-zinc-800 transition-all duration-300">
          <a 
            href="https://dora.run" 
            target="_blank" 
            rel="noopener noreferrer"
          >
            <img src="/images/dora.png" alt="Dora.run" className="w-auto h-12 invert hue-rotate-180 brightness-[1.5] grayscale-[50%] scale-90 object-contain" />
          </a>
        </div>
        <div className="flex items-center justify-center p-4 rounded-lg hover:bg-zinc-800 transition-all duration-300">
          <a 
            href="https://subset.so" 
            target="_blank" 
            rel="noopener noreferrer"
            className="flex items-center gap-3 brightness-[0.8]"
          >
            <img src="/images/subset.png" alt="Subset" className="h-12" />
          </a>
        </div>
        <div className="flex items-center justify-center p-4 rounded-lg hover:bg-zinc-800 transition-all duration-300">
          <a 
            href="https://ax-semantics.com" 
            target="_blank" 
            rel="noopener noreferrer"
          >
            <img src="/images/ax-semantics.svg" alt="ax-semantics" className="w-auto h-12 brightness-[0.8] scale-[0.8]"/>
          </a>
        </div>
        <div className="flex items-center justify-center p-4 rounded-lg hover:bg-zinc-800 transition-all duration-300">
          <a 
            href="https://nemastudio.app/" 
            target="_blank" 
            rel="noopener noreferrer"
            className="flex items-center gap-3 brightness-[0.8]"
          >
            <img src="/images/nema.svg" alt="Nema" className="h-12 invert grayscale-[80%] scale-[0.8]" />
          </a>
        </div>
      </div>
    </section>

    <section className="px-5 md:px-15 mt-20 z-10 mb-20 relative">
      <div className="flex flex-col md:flex-row gap-8 md:[&>*]:w-1/2">
        <div className="p-6 bg-zinc-800 rounded-lg hover:bg-zinc-700/50 cursor-pointer transition-all duration-300 flex flex-col justify-between" 
             style={{minHeight: '250px'}}
             onClick={() => {
               const el = document.getElementById('latch-quote');
               const firstChild = el.children[0];
               const secondChild = el.children[1];
               const firstClass = firstChild.className;
               firstChild.className = secondChild.className;
               secondChild.className = firstClass;
             }}>
          <blockquote id="latch-quote" data-expanded="false" className="text-white/95 italic">
            <div className="block">
              "We use Loro as the document representation for our web-based computational notebook software... This library is a great step forward toward making 'multiplayer' apps the norm."
            </div>
            <div className="hidden">
              "We use Loro as the document representation for our web-based computational notebook software, Latch Plots, via web-assembly. With a CRDT, we can support multiple simultaneous users without having to reinvent the wheel building custom error-prone state management systems. Multi-user interactions 'just work' in a lot of cases and, for ironing out the kinks in the rest, Loro provides state-of-the-art building blocks.<br/><br/>  
              Compared to other CRDT implementations, Loro better matches user expectations and feels more production-ready—the extensive documentation and blogs go into the gory technical details. When the docs are not enough, the Loro team has been fantastic in direct communication—supporting us in designing the state data model and with general issues, and taking our needs and wants into account in planning the project roadmap. <br/><br/> 
              This library is a great step forward toward making 'multiplayer' apps the norm, in a world where the best most users see is still the relatively primitive operational systems like in Google Docs."
            </div>
          </blockquote>
          <div className="mt-4 font-medium text-white/95">
            <div className="flex flex-row gap-4 items-center">
              <img src="/images/max.jpg" alt="Max" className="w-16 h-16 rounded-full object-cover" />
              <div>
                <p>Max</p>
                <p className="text-sm">Latch.bio</p>
              </div>
            </div>
          </div>
        </div>
        <div className="p-6 bg-zinc-800 rounded-lg hover:bg-zinc-700/50 transition-all duration-300 flex flex-col justify-between" 
             style={{minHeight: '250px'}}>
          <blockquote data-expanded="false" className="text-white/95 italic">
            <div className="block">
              "Powerful, performant, and well-designed. The best CRDT for anyone building serious collaborative software."
            </div>
          </blockquote>
          <div className="mt-4 font-medium text-white/95">
            <div className="flex flex-row gap-4 items-center">
              <img src="/images/aj_nandi.jpeg" alt="AJ Nandi" className="w-16 h-16 rounded-full" />
              <div>
                <p>AJ Nandi</p>
                <p className="text-sm">Subset.so</p>
              </div>
            </div>
          </div>
        </div>
        <div className="p-6 bg-zinc-800 rounded-lg hover:bg-zinc-700/50 cursor-pointer transition-all duration-300 flex flex-col justify-between" 
             style={{minHeight: '250px'}}
             onClick={() => {
               const el = document.getElementById('stealth-quote');
               const firstChild = el.children[0];
               const secondChild = el.children[1];
               const firstClass = firstChild.className;
               firstChild.className = secondChild.className;
               secondChild.className = firstClass;
             }}>
          <blockquote id="stealth-quote" data-expanded="false" className="text-white/95 italic">
            <div className="block">
              "After extensive research, we made a big early technology bet on Loro... 10/10 experience to date."
            </div>
            <div className="hidden">
              "After extensive research, we made a big early technology bet on Loro, which we've found to have one of the most thoughtfully designed APIs that we've seen for the otherwise complex world of CRDT-related work. Our team has extensive experience in end-user computing product development, and we've found Loro to be strongly capable at solving many of the shortcomings experienced with other CRDT libraries. While the community is small, the manager of the repo has been remarkably responsive, often submitting same-day bug fixes/feature implementations to ensure that technology choice was not a bottleneck for whatever we decided to build at the product level. 10/10 experience to date."
            </div>
          </blockquote>
          <div className="mt-4 font-medium text-white/95">
            <div className="flex flex-row gap-4 items-center">
              <img src="/images/oliver.jpg" alt="Oliver Beavers" className="w-16 h-16 rounded-full" />
              <div>
                <p>Oliver Beavers</p>
                <p className="text-sm">Stealth Startup</p>
              </div>
            </div>
          </div>
        </div>

      </div>
    </section>
    <section className="px-5 md:px-15 z-10 relative">
      <h2 className="text-2xl not-italic font-semibold leading-[normal] bg-clip-text mb-8 bg-blue-green text-fill-transparent">
        Rich Text Editor Demo
      </h2>
      <Demo />
    </section>


    <section className="px-5 md:px-15 mt-10 z-10 relative">
      <h2 className="text-2xl not-italic font-semibold leading-[normal] bg-clip-text mb-4 bg-blue-green text-fill-transparent">
        Effortless Document Synchronization, <br/>
        Even in P2P Environments
      </h2>
      ```ts
      import { LoroDoc, VersionVector } from "npm:loro-crdt@1.0.0-beta.5";
      const docA = new LoroDoc();
      const docB = new LoroDoc();
      docA.setPeerId(0); // You don't need to set peer id manually
      docA.setPeerId(1); // This example set peer id for deterministic result
      docA.getText("text").insert(0, "Hello!");
      docB.getText("text").insert(0, "Hi!");
      const bytesA: Uint8Array = docA.export({ mode: "update" });
      const bytesB: Uint8Array = docB.export({ mode: "update" });

      // Exchange bytesA and bytesB via any methods
      docB.import(bytesA);
      docA.import(bytesB);

      // The merge result is consistent
      console.log(docA.getText("text").toString()); // Hello!Hi!
      console.log(docB.getText("text").toString()); // Hello!Hi!
      ```

    <div className="mt-8 flex">
      <a
        href="/docs/tutorial/get_started"
        className="px-4 py-2 text-base font-medium text-white bg-zinc-800 hover:bg-zinc-700 transition duration-300 ease-in-out rounded-md"
      >
        Get Started
      </a>
    </div>
    </section>

    <Support />
    <div className="mb-20" />
    <section className="mt-10 flex flex-col items-center justify-center space-x-2.5">
      <div>
        <FollowOnGitHub />
      </div>
    </section>

  </div>
  <Separator
    className="mt-25 mx-5 md:mx-15 border-t border-separator"
    orientation="horizontal"
  />
  <Footer />
</div>
